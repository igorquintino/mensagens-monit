# Usando a imagem oficial do Node.js como base
FROM node:18

# Definindo o diretório de trabalho dentro do contêiner
WORKDIR /app

# Copiando arquivos do projeto para dentro do contêiner
COPY . .

# Atualizando pacotes e instalando dependências necessárias para o Puppeteer e VenomBot
RUN apt-get update && apt-get install -y     libx11-xcb1     libasound2     libnss3     libxss1     libappindicator3-1     libatk-bridge2.0-0     libgbm-dev     && rm -rf /var/lib/apt/lists/*

# Instalando as dependências do Node.js
RUN npm install

# Expondo a porta necessária para o funcionamento do bot (caso precise)
EXPOSE 3000

# Comando para iniciar o monitor de preços
CMD ["node", "monitor.js"]
